{
    "name": "whois-assignment",
    "private": true,
    "version": "1.0.0",
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "lint": "eslint .",
        "preview": "vite preview",
        "start": "concurrently \"npm run server\" \"npm run client\"",
        "server": "node server/server.js",
        "client": "npm run dev --prefix client"
    },
    "dependencies": {
        "concurrently": "^9.1.0"
    },
    "devDependencies": {
        "wait-on": "^8.0.1"
    }
}
